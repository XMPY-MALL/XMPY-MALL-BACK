spring:
  profiles:
    active: common # 현재 프로필 설정
    group: # 특정 프로필을 실행할때, 같이 실행되게끔 그룹 짓기
      common: secret # common이 실행되면. secret 프로필 설정

---

spring:
  config:
    activate:
      on-profile: common

  application:
    name: spring-boot-study
# xmpy-mall 서버 연결 완료

  datasource:
    url: jdbc:mysql://localhost:3306/xmpy
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1q2w3e4r!

mybatis:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.xmpy.demo.entity
# mapper 연결 완료

jwt: # 시크릿 난수 생성 터미널 명령어 : openssl rand -base64 64
  secret: "XLsJxKSTztZmOcjPj8qQNZpirpiuW3uPTuPeAI/ZkWMRvUGf+yi3VwnrLst8/WMRj1VBKHA0AQ2hYVNubcL8Tw=="
  access-expire-millis: 900000 # 15분 -> ms
  refresh-expire-millis: 86400000 # 1일 1 * 24 * 60 * 60 * 1000